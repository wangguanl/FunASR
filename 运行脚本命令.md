# FunASR 运行脚本命令

本机环境：Windows / PowerShell 5 / Python 3.10.11（uv 虚拟环境）/ RTX 4080（16GB 显存）

所有命令需在项目根目录 `E:\AI\local-voice\FunASR` 下执行。

- FunASR 识别 / API：用本仓库 `.venv`（`.\\.venv\\Scripts\\python.exe`）
- 音频分离界面：用 `E:\AI\audio-demucs\python-audio-separator\.venv`（`start.ps1` 已写死）

---

## 1. 启动 FunASR API 服务（端口 8000）

OpenAI 兼容 API（FastAPI），提供 `/v1/audio/transcriptions`、`/v1/models`、`/health` 端点。

```powershell
# 默认：sensevoice 模型，GPU 推理，端口 8000
& ".\.venv\Scripts\python.exe" examples\openai_api\server.py --model sensevoice --device cuda --port 8000

# CPU 推理（无 GPU 时）
& ".\.venv\Scripts\python.exe" examples\openai_api\server.py --model sensevoice --device cpu --port 8000
```

- API 地址：http://localhost:8000
- Swagger 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health
- 可选模型：`sensevoice`、`paraformer`、`paraformer-en`、`fun-asr-nano`

---

## 2. 启动 FunASR Gradio WebUI（端口 47821）

完整功能图形界面（上传/录音 → 转写、情感/事件、说话人分离、标点、时间戳、热词等）。

脚本默认端口是 **47821**（`funasr_webui.py`），不要再按 7860 访问。

```powershell
# 默认：GPU 推理，端口 47821
& ".\.venv\Scripts\python.exe" funasr_webui.py --host 0.0.0.0 --device cuda

# 显式指定端口（与默认相同）
& ".\.venv\Scripts\python.exe" funasr_webui.py --host 0.0.0.0 --port 47821 --device cuda

# CPU 推理
& ".\.venv\Scripts\python.exe" funasr_webui.py --host 0.0.0.0 --port 47821 --device cpu

# 自定义端口
& ".\.venv\Scripts\python.exe" funasr_webui.py --port 8001 --device cuda

# 生成临时公网链接（--share）
& ".\.venv\Scripts\python.exe" funasr_webui.py --host 0.0.0.0 --port 47821 --device cuda --share
```

- WebUI 地址：http://localhost:47821

---

## 3. 常用 curl 测试命令

### 3.1 健康检查

```powershell
Invoke-RestMethod -Uri http://localhost:8000/health
```

### 3.2 转写本地音频（OpenAI 兼容）

```powershell
curl.exe http://localhost:8000/v1/audio/transcriptions `
  -F "file=@audio.wav" -F "model=sensevoice"

# 指定语言 + verbose_json（含时间戳/说话人）
curl.exe http://localhost:8000/v1/audio/transcriptions `
  -F "file=@audio.wav" -F "model=sensevoice" `
  -F "language=zh" -F "response_format=verbose_json"
```

### 3.3 列出可用模型

```powershell
Invoke-RestMethod -Uri http://localhost:8000/v1/models
```

---

## 4. 启动音频分离界面（端口自动分配）

基于 `python-audio-separator`（Demucs）的本地音频分离界面，上传音频即可分离人声/伴奏等音轨。
已内置 24bit 音频修复（上传自动转 16bit WAV）、ffmpeg 环境、模型自动检查下载。

```powershell
# 方式一：根目录 start.ps1（推荐）
.\start.ps1

# 方式二：命令行直接运行自包含脚本
& "E:\AI\audio-demucs\python-audio-separator\.venv\Scripts\python.exe" separator_webui.py
```

- 优先尝试 http://127.0.0.1:7860；被占用则自动顺延（7861、7862…），终端会打印实际地址，浏览器按实际端口打开。
- 模型缓存：`C:\Users\wang\audio-separator-models`（缺失时自动经加速镜像下载，约 610MB）
- 与 FunASR WebUI（默认 47821）不冲突，可以同时开。

---

## 5. 其他相关脚本

| 脚本 | 用途 |
|------|------|
| `funasr_webui.py` | FunASR 完整功能 Gradio 界面（语音识别，默认端口 47821） |
| `separator_webui.py` | 音频分离自包含脚本（配合 `start.ps1` 使用） |
| `start.ps1` | 音频分离一键启动入口 |
| `examples\openai_api\server.py` | OpenAI 兼容转写 API（端口 8000） |
| `examples\openai_api\gradio_app.py` | 官方 API 演示界面（精简） |

---

## 6. 停止服务

在对应终端中按 `Ctrl+C` 停止进程。

---

## 7. 注意事项

- 首次运行会自动从 ModelScope 下载模型（SenseVoice ~936MB、FSMN-VAD 等），缓存于 `C:\Users\wang\.cache\modelscope`。
- WebUI 首屏提示 `theme` 参数位置警告是 Gradio 6 的正常提醒，不影响使用。
- 本机显存上限 16GB，多模型同时加载时注意显存占用；必要时改用 `--device cpu` 或单个模型。
